import { PublicInvoiceView } from '@/components/features/invoices/PublicInvoiceView';

export const metadata = { title: 'Review invoice', referrer: 'no-referrer' };

export default function PublicInvoicePage() {
  return <PublicInvoiceView />;
}
